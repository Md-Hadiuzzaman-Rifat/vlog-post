import Loading from '@/components/loading/Loading';
import React from 'react';

const loading = () => {
    return (
        <div>
            <Loading></Loading>
        </div>
    );
};

export default loading;